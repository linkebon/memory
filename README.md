# Memory game

## Requirements
* elixir 1.10.4 or newer

## Run game
Compile game by running following in shell:
* `iex`
* `c "card.ex"`
* `c "game_logic.ex"`
* `c "memory.ex"`

Run game:
* `Memory.run_memory`